# 本地啟動說明

## 快速啟動（推薦）

直接執行啟動腳本：
```bash
start_local.bat
```

這個腳本會自動：
1. 安裝 `uv` 套件管理器（如果尚未安裝）
2. 建立虛擬環境 `.venv`（如果尚未建立）
3. 在虛擬環境中安裝所有依賴
4. 啟動 Lite Orchestrator

## 手動啟動步驟

如果您想手動控制每個步驟：

### 1. 安裝 uv
```bash
python -m pip install uv
```

### 2. 建立虛擬環境
```bash
python -m uv venv .venv
```

### 3. 啟動虛擬環境
```bash
.venv\Scripts\activate.bat
```

### 4. 安裝依賴
```bash
python -m uv pip install -r requirements/core.txt
python -m uv pip install -r requirements/downloader.txt
python -m uv pip install -r requirements/analysis.txt
```

### 5. 啟動服務
```bash
python src/core/orchestrator_lite.py
```

## 啟動後

服務啟動後，您會在終端看到類似以下的訊息：
```
✅ 資料庫管理器已就緒 (Port: 50001)
✅ API 伺服器已就緒 (Port: 8000)
==================================================
🎉 系統啟動完成！請在瀏覽器中開啟以下網址：
👉 http://127.0.0.1:8000
==================================================
```

在瀏覽器中開啟顯示的網址即可使用。

## 停止服務

在終端按 `Ctrl+C` 即可停止服務。
