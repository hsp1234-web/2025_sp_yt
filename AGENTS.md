# AI 代理協作指南

歡迎！為了確保我們之間協作順暢，請您在開始任何任務前，仔細閱讀並遵守以下準則。

> [!IMPORTANT]
> **⚠️ 請保持根目錄整潔**：
> *   **核心程式碼**：必須存放在 `src/` 目錄下（含 api, core, db, tools, static 等子目錄）。
> *   **工具腳本**：維護與自動化相關的腳本應存放在 `scripts/` 目錄下。
> *   **設定檔案**：所有設定檔應存放在 `config/` 目錄下。
> *   **文件資料**：所有文件應存放在 `docs/` 目錄下。
> *   **下載產出**：下載的音訊、影片、報告存放在 `downloads/` 目錄下。
> *   **封存檔案**：不再使用但需保留的舊檔案存放在 `_archive/` 目錄下。
> *   **嚴禁**將程式碼檔案或產出物直接散落在根目錄（除了 `main.py`、啟動腳本、設定檔）。

## 📂 目錄結構說明

```
YouTube_download/
├── src/                    # 核心程式碼
│   ├── api/               # API 路由與伺服器
│   ├── core/              # 核心邏輯模組
│   ├── db/                # 資料庫相關
│   ├── tools/             # 工具模組（下載器、轉錄器等）
│   ├── static/            # 前端靜態檔案（HTML, CSS, JS）
│   └── prompts/           # 提示詞設定
├── scripts/               # 工具腳本
├── config/                # 設定檔
├── docs/                  # 文件
│   ├── logs/             # 開發日誌（按月份分類）
│   ├── Project_Plans/    # 專案計畫
│   ├── Research_Reports/ # 研究報告
│   └── System_Documentation/ # 系統文件
├── downloads/             # 下載的檔案
│   ├── audio/            # 音訊檔案
│   ├── video/            # 影片檔案
│   └── reports/          # 產生的報告
├── data/                  # 數據資料
├── requirements/          # 依賴套件清單
├── uploads/               # 使用者上傳檔案（不納入版控）
├── _archive/              # 封存的舊檔案
└── backup/                # 備份（不納入版控）
```

## 溝通語言

*   **全程使用繁體中文**：所有的溝通、程式碼註解、提交訊息、以及產出的文件，都必須使用繁體中文。

## 工作流程

1.  **理解與規劃**：在收到任務後，請先充分探索與分析，並提出一份詳細的執行計畫。
2.  **使用者批准**：**絕對禁止**在沒有獲得使用者明確批准之前，對任何檔案進行修改。計畫必須先獲得同意。
3.  **執行與驗證**：在獲得批准後，依照計畫執行。每一步完成後，都應進行驗證，確保結果符合預期。

## 開發日誌維護

*   **使用結構化日誌系統**：所有開發日誌都應記錄在 `docs/logs/` 目錄中，每個日誌都是獨立的 Markdown 檔案。
*   **檔名格式**：`YYYY-MM-DD_HH-MM_brief-description.md`（例如：`2025-11-22_20-15_v9.1_structure_optimization.md`）
*   **目錄結構**：日誌按月份分類存放於 `docs/logs/YYYY-MM/` 目錄下。
*   **建立新日誌**：
    *   **推薦方式**：使用 `python scripts/create_log.py "簡短描述"` 自動產生日誌檔案
    *   **手動方式**：參考 `docs/logs/README.md` 中的格式規範
*   **時區標準**：記錄時間戳記時，必須使用「亞洲/台北」時區（UTC+8）。
*   **內容規範**：每個日誌應包含標題、日期、作者、摘要、主要變更、驗證結果等章節。

## 📚 歷史紀錄與日誌查詢

為了讓其他 AI 工具或團隊成員能快速查詢過往的工作紀錄，我們將所有開發、執行與分析的日誌統一放在 `docs/logs/` 目錄下。

日誌內容應包含：
- 任務名稱與目標
- 執行步驟與指令
- 產生的檔案或結果摘要
- 任何錯誤或需注意的事項

其他 AI 工具只要讀取 `docs/logs/` 內的檔案，即可取得完整的歷史脈絡，避免重複工作。

## 🔧 Git 版控注意事項

1.  **中文檔名**：專案中有中文檔名，請確保 Git 設定正確：
    ```bash
    git config core.quotepath false
    ```

2.  **不納入版控的目錄**：
    - `uploads/` - 使用者上傳檔案
    - `backup/` - 備份檔案
    - `*.db`, `*.sqlite3` - 資料庫檔案
    - `config.json` - 含有敏感資訊的設定

3.  **提交訊息格式**：使用繁體中文，簡潔描述變更內容。

感謝您的合作！
