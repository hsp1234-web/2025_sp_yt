# 🚀 快速啟動指南

## ⚡ 一鍵啟動（推薦）

雙擊執行：**`啟動服務.bat`**

這個批次檔會：
1. 開啟一個終端視窗
2. 在 WSL 中啟動服務
3. **保持視窗開啟**（服務會持續運行）
4. 顯示存取網址

## 📋 啟動後的步驟

1. 等待終端顯示類似以下訊息：
   ```
   ✅ API 伺服器已就緒 (Port: 8000)
   👉 http://127.0.0.1:8000
   ```

2. 在瀏覽器中開啟：`http://127.0.0.1:8000`

3. **重要**：不要關閉終端視窗！關閉視窗會停止服務。

## ⏹️ 停止服務

在終端視窗中按 `Ctrl+C`，或直接關閉視窗。

## 🔧 如果遇到問題

### 問題 1：依賴未安裝
在 WSL 中手動安裝：
```bash
cd /mnt/c/SP_DOC/YouTube_download
pip3 install -r requirements/core.txt
pip3 install -r requirements/downloader.txt
pip3 install -r requirements/analysis.txt
```

### 問題 2：Port 8000 被佔用
檢查並終止佔用的進程：
```bash
wsl lsof -i :8000
wsl kill -9 <PID>
```

### 問題 3：Python 未安裝
在 WSL 中安裝 Python：
```bash
sudo apt update
sudo apt install python3 python3-pip
```
