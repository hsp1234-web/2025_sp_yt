# 專案環境自動設定指南

## 概覽

本文件旨在說明如何使用 `setup_environment.sh` 這個自動化腳本，來快速地為您的開發環境安裝所有必要的依賴。

我們在開發過程中，發現系統需要許多特定的系統級函式庫（如 Chrome、Playwright 依賴）和大量的 Python 套件。為了避免團隊成員在設定新環境時，重複我們之前耗時的除錯過程，我們將所有步驟都固化到了這個腳本中。

## 用途

執行此腳本將會自動完成以下所有設定：

1.  **更新系統**: 使用 `apt-get update` 更新您系統的套件列表。
2.  **安裝核心應用**: 自動安裝 Google Chrome 瀏覽器和 `npm` (Node.js 套件管理器)。
3.  **安裝瀏覽器依賴**: 自動安裝數百個 Playwright 驅動瀏覽器所需的所有圖形、多媒體和字體相關的函式庫。
4.  **安裝 Python 套件**: 使用 `uv` 高效地安裝專案根目錄下 `requirements.txt` 總表中定義的所有 Python 套件。
5.  **設定 Playwright**: 自動下載並設定 Playwright 所需的瀏覽器核心。

## 使用方法

### 前提條件

-   您的作業系統是基於 **Debian** 或 **Ubuntu** 的發行版。
-   您擁有 `sudo` (系統管理員) 權限，因為腳本需要安裝系統級的套件。

### 執行步驟

1.  打開您的終端機 (Terminal)。
2.  `cd` 到本專案的根目錄 (也就是您看到這個 `SETUP_GUIDE.md` 檔案的目錄)。
3.  執行以下指令：

    ```bash
    ./setup_environment.sh
    ```

4.  腳本會開始執行，並顯示每個步驟的進度。您只需要等待它執行完畢即可。當您在最後看到 `✅✅✅ 環境設定完成！` 的訊息時，就代表您的環境已經完全準備就緒了。

---

透過這個一鍵式的腳本，我們可以確保團隊所有成員都擁有一致且完整的開發環境，從而將時間專注在更有價值的開發工作上。