# 本地 OCR 解決方案概念驗證與模型評測報告

**日期:** 2025-10-09
**作者:** Jules
**狀態:** 已完成

## 1. 專案背景與目標

根據使用者需求，我們需要在現有的卡片介面上，增加由本地 AI 執行的光學字元辨識 (OCR) 與智慧分析功能。本次研究的核心目標是：

> 尋找一個高效、準確且能在 CPU 環境下運行的 **本地 OCR 解決方案**，並為後續開發提供數據支持和技術選型建議。

## 2. 初步技術驗證 (POC): Ollama + Llava

我們的第一步是驗證專案中現有的本地 AI 框架 `Ollama` 是否具備完成此任務的潛力。

- **技術選型**: 我們選用了 `llava:latest`，這是一個通用的多模態視覺模型，理論上具備看懂圖片並讀取文字的能力。
- **POC 執行過程**:
    1. **環境搭建**: 過程中我們解決了多個環境問題，包括：
        - 安裝 `ollama` Python 客戶端套件。
        - 執行 `install_ollama.sh` 腳本來安裝並啟動 Ollama 服務。
        - 解決了服務啟動與指令執行的「競態條件」問題，確保服務完全就緒。
        - 透過 `apt` 安裝了 `fonts-noto-cjk` 字體包，並使用 `Pillow` 函式庫成功生成了標準化的測試圖片 `test_ocr_image.png`，以供後續所有模型使用。
    2. **模型下載**: 我們使用 `ollama pull llava:latest` 指令成功下載了所需的 AI 模型。
    3. **執行驗證**: 最終，我們成功執行了 `poc_ollama_ocr.py` 腳本，將測試圖片傳送給本地的 Llava 模型。
- **POC 結論**:
    > **成功**。Llava 模型幾乎完美地辨識出了測試圖片中的所有繁體中文及英數文字。這強而有力地證明了**使用 Ollama 作為本地 AI 推論引擎的技術路線是完全可行的**。

## 3. 擴大研究：尋找更專業的 OCR 模型

在 Llava 成功的基礎上，為了追求極致的效能與準確度，我們遵循您的指示，啟動了更廣泛的市場調研，旨在尋找並評測更專業、更專注於 OCR 任務的開源模型。

我們主要研究了以下幾個在開源社群中備受關注的候選者。

## 4. 候選模型特性分析

| 模型名稱 | 技術路線 | 優點 | 缺點 | 結論 |
| :--- | :--- | :--- | :--- | :--- |
| **OCRFlux-3B** | Ollama 模型 | 1. **原生整合**: 可在 Ollama 中一鍵下載使用。<br>2. **專為 OCR 設計**: 目標是將圖片轉為乾淨文字。<br>3. **架構統一**: 與我們現有技術棧完全吻合。 | 作為較新模型，實際效能待驗證。 | **第一候選者** |
| **EasyOCR** | 獨立 Python 套件 | 1. **極易使用**: API 設計簡潔，專為 Python 開發者打造。<br>2. **CPU 友好**: 文件明確提供 `gpu=False` 選項。<br>3. **社群成熟**: 廣泛使用，文件和解決方案豐富。 | 需獨立管理其依賴 (主要是 PyTorch)，與 Ollama 分離。 | **第二候選者** |
| **Tesseract** | 傳統 OCR 引擎 | 1. **業界標竿**: Google 維護，歷史悠久，極其穩定。<br>2. **高度可靠**: 官方提供繁體中文語言包。 | 1. **安裝複雜**: 需同時處理系統級和 Python 級的安裝。<br>2. **效能可能落後**: 相較於新興的深度學習模型，其速度和準確度存疑。 | **第三候選者** |
| **MonkeyOCR** | Python / AI 模型 | 功能強大，專注於**結構化**資料提取（如發票欄位）。 | 對於我們「通篇辨識」的需求而言，功能過於複雜 (Overkill)。 | 暫不考慮 |
| **olmOCR** | 獨立工具 | 由 Allen AI 開發，準確度高。 | 未發現與 Ollama 的整合方法，作為獨立工具整合成本高。 | 暫不考慮 |

## 5. 最終測試方案

基於以上研究，我們最終選定了三款代表不同技術路線的頂級模型進入最終的基準測試環節：`OCRFlux-3B`, `EasyOCR`, `Tesseract`。

為了確保測試的公平性和標準化，我們已經開發了 `benchmark_ocr_models.py` 腳本。此腳本將在完全相同的環境下，使用同一張測試圖片，對三個模型的**辨識準確度**和**執行速度**進行量化評估。

## 6. 後續步驟建議

雖然本次的實際測試因環境問題而中斷，但我們已經完成了所有必要的研究和準備工作。後續的核心任務將是修復環境問題，並執行已開發完成的 `benchmark_ocr_models.py`，以獲取最終的性能數據，從而為您的專案做出最佳的技術選型。