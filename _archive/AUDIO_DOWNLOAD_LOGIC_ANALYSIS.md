# 音訊下載邏輯綜合分析報告

## 總覽

本系統的音訊（及影片）下載功能，雖然從使用者介面上看有多個入口，但其底層實現高度統一，最終都依賴一個核心的下載工具腳本。本報告旨在詳細闡述從前端觸發到後端處理，再到最終檔案落地的完整流程。

---

## 兩大主要流程

系統中主要有兩個發起下載的流程，它們服務於不同的功能模組，但最終都匯集到同一個核心下載器。

### 1. 通用媒體下載器

-   **前端入口**: `src/static/mp3.html` 內的「媒體下載器」分頁。
-   **使用者操作**: 使用者在文字輸入框中貼上單一或多個影音網址，選擇下載類型（音訊/影片）及格式，然後點擊「開始下載」。
-   **後端 API**: 觸發後端 `/api/youtube/process` 端點。
-   **核心工具**: 該 API 內部會呼叫 `src/tools/youtube_downloader.py` 腳本來執行實際的下載任務。
-   **狀態更新**: 透過 WebSocket 將即時進度回傳給前端介面，並顯示在「下載佇列與歷史紀錄」區域。

### 2. 音訊報告生成器模組

-   **前端入口**: `src/static/audio_report_1_source.html` (步驟一：獲取媒體來源)。
-   **使用者操作**: 此為一個獨立的多頁面功能模組。使用者在此頁面輸入網址並啟動下載。下載完成後，檔案會進入一個「暫存區」，使用者可在「步驟二：分析與處理」頁面 (`audio_report_2_process.html`) 中選擇該檔案進行後續的 AI 分析。
-   **後端 API**: 觸發專為此模組設計的 `/api/audio_report/download` 端點（其實作位於 `src/api/routes/audio_report_api.py`）。
-   **核心工具**: 與通用下載器一樣，此 API 也是透過建立系統子進程 (subprocess) 的方式來執行 `src/tools/youtube_downloader.py` 腳本。
-   **狀態更新**: 同樣使用專屬的 WebSocket (`/api/audio_report/ws`) 將進度回傳給前端，並在下載完成後提供「下一步」的按鈕，引導使用者進入分析流程。

---

## 核心下載腳本：`youtube_downloader.py`

不論請求源自何處，所有下載任務的最終執行者都是位於 `src/tools/` 目錄下的 `youtube_downloader.py` 腳本。這確保了下載邏輯的統一性和可維護性。

其詳細工作流程如下：

1.  **參數接收**: 腳本透過命令列參數（`argparse`）從呼叫它的後端 API 接收所有必要的設定，例如：
    *   `--url`: 目標媒體的網址。
    *   `--output-dir`: 檔案儲存的目錄。
    *   `--download-type`: `audio` 或 `video`。
    *   `--audio-format`: `m4a`, `mp3` 等。
    *   `--video-resolution`: `best`, `1080p` 等。

2.  **呼叫 `yt-dlp`**: 腳本的核心是構建一個命令列指令來執行 `yt-dlp`。`yt-dlp` 是一個功能強大的開源命令列工具，專門用於從 YouTube、Bilibili、Facebook 等數百個網站下載影音內容。腳本會根據傳入的參數，組合出對應的 `yt-dlp` 指令選項（例如 `-x --audio-format mp3` 用於提取 MP3 音訊）。

3.  **檔案命名策略**: 這是該腳本的一個關鍵特性。它採用「**先下載，後改名**」的策略，以確保檔名的規範性和可追溯性。
    *   **步驟 A (下載)**: 指示 `yt-dlp` 使用影片的原始標題 (`%(title)s`) 作為檔名進行下載。例如，一個標題為「我的演講」的影片會被下載為 `我的演講.m4a`。
    *   **步驟 B (重新命名)**: 在 `yt-dlp` 成功執行後，腳本會從其 JSON 輸出中獲取已儲存的實際檔案路徑。接著，腳本會對該檔案進行**重新命名 (rename)**，在原始檔名的基礎上，於最前方加入一個格式標籤。例如，`我的演講.m4a` 會被改名為 `[m4a]我的演講.m4a`。

4.  **結果回傳**: 腳本執行完成後，會將一個包含詳細結果的 JSON 物件印出到標準輸出 (stdout)。這個 JSON 物件包含了如下關鍵資訊：
    *   `status`: "已完成" 或 "failed"。
    *   `output_path`: 最終儲存的完整檔案路徑（例如 `downloads/audio/[m4a]我的演 usada.m4a`）。
    *   `video_title`: 媒體的原始標題。
    *   `duration_seconds`: 媒體時長。

5.  **流程閉環**: 呼叫此腳本的後端 API (如 `audio_report_api.py`) 會捕捉這個標準輸出，解析其中的 JSON 數據，並將這些結果透過 WebSocket 傳送給前端，從而完成整個流程的閉環。前端介面根據這些數據來更新 UI，例如顯示下載成功的檔案、提供預覽或下一步操作的按鈕。

## 結論

系統的下載架構設計良好，將使用者介面、後端邏輯和核心下載工具進行了有效分離。儘管有兩個不同的功能模組提供了下載入口，但它們共享同一個 `youtube_downloader.py` 核心腳本，確保了下載行為的一致性。檔案命名策略清晰，透過在檔名前添加格式標籤，使得使用者和系統都能輕易識別檔案類型。
